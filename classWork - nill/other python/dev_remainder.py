#dev_remainder.py

first_number = (int(input("input a number here: ")))
second_number = (int(input("input a number here: ")))

maths = (first_number%second_number)

#remainder = first_number - maths

print(maths)